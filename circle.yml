machine:
  python:
    version: 3.6.1
dependencies:
  pre:
    - pip install -r requirements.txt
    - cookiecutter --no-input
    - cd mo-static; yarn
test:
  override:
    - pytest
    - cd mo-static; npm test
